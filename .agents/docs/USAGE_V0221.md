# Usage v0.22.1

Use `db-identity-policy-*`, `db-identity-resolution-*`, `db-identity-candidate-*`, `db-identity-readiness`, and `db-entity-lineage-show` through `.agents/bin/agentos`.

Mutation commands are CLI/operator only. MCP is read-only.
